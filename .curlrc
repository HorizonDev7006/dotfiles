# follow redirects automatically
-L

# show a simple progress bar instead of full details
--progress-bar

# set user agent like a legit browser so servers don’t block you
-A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/115.0.0.0 Safari/537.36"

# timeout after 15 seconds so it doesn’t hang forever
--max-time 15

# save cookies to file automatically, so session stays alive across runs
-c ~/.curl_cookies.txt

# read cookies from file automatically
-b ~/.curl_cookies.txt

# follow location for redirects
-L
